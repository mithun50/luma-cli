<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Block cleartext by default for security -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only for localhost and private network ranges -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Localhost -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>

        <!-- Android emulator host -->
        <domain includeSubdomains="false">10.0.2.2</domain>

        <!-- Private network ranges (Class A, B, C) -->
        <!-- 10.x.x.x -->
        <domain includeSubdomains="true">10.0.0.1</domain>
        <!-- 172.16.x.x - 172.31.x.x -->
        <domain includeSubdomains="true">172.16.0.1</domain>
        <!-- 192.168.x.x -->
        <domain includeSubdomains="true">192.168.0.1</domain>
        <domain includeSubdomains="true">192.168.1.1</domain>

        <!-- Ngrok tunnels (HTTP fallback, though usually HTTPS) -->
        <domain includeSubdomains="true">ngrok.io</domain>
        <domain includeSubdomains="true">ngrok-free.app</domain>
    </domain-config>
</network-security-config>
